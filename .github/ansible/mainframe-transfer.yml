---
- hosts: ben_s0w1 
  collections:
    - ibm.ibm_zos_core
  gather_facts: no

  tasks:
    - name: Transfer file to mainframe PDS dataset
      zos_copy:
        src: .github/ansible/mainframe-transfer.yml
        dest: 'IBMUSER.DBB.COBOL(MEMBER)'
        is_binary: yes